# ربات مدیریت اشتراک تلگرام برای پرتفوی معاملاتی

این پروژه یک ربات تلگرام پیشرفته برای مدیریت اشتراک کاربران، پرداخت‌ها، ثبت فیش، مشاهده وضعیت پرتفوی معاملاتی و ارسال تسویه هفتگی است. این ربات به صورت کامل با معماری `python-telegram-bot` طراحی شده و قابل توسعه برای کسب و کارهای حقیقی می‌باشد.

## 🎥 دمو و عملکرد ربات

در این ویدیو توضیح داده‌ام چطور ربات کار می‌کند، چه امکاناتی دارد و چگونه از آن استفاده کنید:

➡️ لینک ویدیو: *اینجا لینک ویدیو را قرار دهید*

---

## 📁 ساختار پروژه
```bash
telegram-subscription-bot/
├── app.py # فایل اصلی ربات
├── db.py # منطق دیتابیس SQLite
├── handlers/ # مدیریت رویدادها و منوهای ربات
│ ├── common.py
│ ├── registration.py
│ ├── payments.py
│ ├── admin.py
│ ├── portfolio.py
│ └── report.py
├── scheduler/ # زمان‌بندی تسویه‌ها
├── config.py # تنظیمات و پارامترها (توکن تلگرام و ...)
└── README.md # همین فایل
```

---

## ⚙️ راه‌اندازی پروژه

1. ابتدا کتابخانه‌های مورد نیاز را نصب کنید:
   ```bash
   pip install -r requirements.txt
   ```
فایل config.py را ایجاد کنید و مقدار زیر را وارد کنید:
   ```bash
      BOT_TOKEN = "توکن ربات تلگرام"
      ADMIN_IDS = [12345678]
      SCHEDULER_INTERVAL = 3600  # فاصله زمانی برای چک عضویت‌ها (به ثانیه)
```
اجرا کنید:
   ```bash
     python main.py
```
📬 خرید و همکاری

اگر علاقه‌مند به اجرای کامل این پروژه با تمام فایل‌های کد و سازوکار راه‌اندازی هستید، لطفاً از طریق لینک‌های زیر با من تماس بگیرید:

تلگرام: @mhshirin


